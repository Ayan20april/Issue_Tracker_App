<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Issue Tracker</title>
</head>
<body>
  <h1>Issue Tracker</h1>

  //takes the user to the "Create New Issue" page. It's a hyperlink that leads to the /create route.
  <a href="/create">Create New Issue</a>
  <ul>

    //This is an embedded JavaScript code block that iterates through the issues array and generates HTML for each issue.
    <% issues.forEach(issue => { %>
      <li>
        <a href="/issue/<%= issue.id %>"><%= issue.title %></a>
        <form action="/delete/<%= issue.id %>" method="post" style="display: inline;">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>
